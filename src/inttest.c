#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

int main() {
  printf("sizeof(uint8_t) == %lu\n", sizeof(uint8_t));
  printf("sizeof(uint16_t) == %lu\n", sizeof(uint16_t));
  printf("sizeof(uint32_t) == %lu\n", sizeof(uint32_t));
  printf("sizeof(uint64_t) == %lu\n", sizeof(uint64_t));
}
